/**
 * `@jeswr/agentic-legacy-bridge/email` — the standalone, dependency-light email
 * parsing surface. Importable on its own for callers that only want the hardened
 * RFC 5322 / MIME parse (no RDF / pod machinery).
 *
 * @packageDocumentation
 */
export { EmailParseError, parseEmail } from "./parse.js";
export type { EmailAddress, EmailMessage } from "./types.js";
//# sourceMappingURL=index.d.ts.map